{
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "nodemon": "^3.1.4"
  },
  "name": "backend",
  "description": "nmp init -y",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon app.js",
    "migrate": "prisma migrate dev --name"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prisma": "^5.16.0"
  }
}
